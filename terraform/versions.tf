# Terraform backend configuration
# The provider configuration is in main.tf to avoid duplication

# Uncomment and configure if you want to use a remote backend
# terraform {
#   backend "gcs" {
#     bucket = "your-terraform-state-bucket"
#     prefix = "terraform/state"
#   }
# }
